[package]
name = "libpcap"
version = "0.1.0"
authors = ["Jack Liar"]
edition = "2018"

[lib]
crate-type = ["cdylib", "rlib"]
name = "rxlibpcap"

[dependencies]
alphonse-api = { path = "../../api", optional = false }
anyhow = "1.0"
crossbeam-channel = "0.5"
libc = "0.2"
pcap = "0.8"
serde = "1.0"
